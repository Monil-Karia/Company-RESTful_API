from django.apps import AppConfig

class ApiConfig(AppConfig):
    # The default auto-generated primary key field for models
    default_auto_field = "django.db.models.BigAutoField"

    # The name of the Django app
    name = "api"
